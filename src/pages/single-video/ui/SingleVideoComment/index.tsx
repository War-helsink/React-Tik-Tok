import type { FC } from "react";

const SingleVideoComment: FC = () => {
	return (
		<>
			<section className="flex-grow-0 w-[550px] h-full">Comment</section>
		</>
	);
};

export default SingleVideoComment;
